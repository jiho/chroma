# DO NOT EDIT THIS FILE!
# Edit scale_viridis.R and run ./scale_viridis_variants.sh

#' Magma colors
#'
#' The \code{plasma} color palette, by Nathaniel J. Smith and Stefan van der Walt (CC0 license).
#'
#' @format A vector of length 256 containing hex values.
#' @source \url{https://bids.github.io/colormap/} for the concept and \url{https://github.com/BIDS/colormap/blob/master/colormaps.py} for the data.
#' @family mpl palettes
"plasma"


#' Magma color scale and palette
#'
#' The \code{plasma} color palette, by Nathaniel J. Smith and Stefan van der Walt (CC0 license).
#'
#' @inheritParams make_scale 
#'
#' @template return_scales
#'
#' @family color scales and palettes
#'
#' @examples
#' # get a few colors along the palette
#' show_col(
#'   plasma_palette()(20),
#'   plasma_colors(50),
#'   plasma_colors(20, reverse=TRUE)
#' )
#' 
#' # Maunga Whau volcano elevation map
#' x <- 10*(1:nrow(volcano))
#' y <- 10*(1:ncol(volcano))
#' image(x, y, volcano, col=plasma_colors(100))
#' persp(x, y, volcano, theta=60, phi=25, border=NA,
#'       col=plasma_map(persp_facets(volcano)))
#' \dontrun{
#' library("rgl")
#' persp3d(x, y, volcano, aspect=c(1,0.6,0.3), axes=FALSE, box=FALSE,
#'         col=plasma_map(volcano))
#' play3d(spin3d(axis=c(0, 0, 1), rpm=10), duration=6)
#'
#' }
#' # with a limited number of levels, plasma can also serve as a discrete
#' # color scale
#' attach(iris)
#' plot(Petal.Length, Sepal.Length, pch=19, col=plasma_map(Species))
#' legend(1, 8, legend=levels(Species), pch=19, col=plasma_colors(n=nlevels(Species)))
#'
#' @export
plasma_scale <- function(domain=c(0,1), reverse=FALSE) {
  make_scale(colors=chroma::plasma, model="lab", interp="linear", domain=domain, reverse=reverse)
}

#' @param ... passed to \code{\link{plasma_scale}}. Note that argument \code{domain} is meaningless in functions other than \code{plasma_scale} and passing it through \code{...} is an error.
#' @name plasma_scale
#' @export
plasma_palette <- function(...) {
  make_palette(colors=chroma::plasma, model="lab", interp="linear", ...)
}

#' @name plasma_scale
#' @export
plasma_colors <- function(n, ...) {
  plasma_colors(n=n, colors=chroma::plasma, model="lab", interp="linear",...)
}
#' @name plasma_scale
#' @export
plasma.colors <- plasma_colors

#' @name plasma_scale
#' @export
plasma_map <- function(x, ...) {
  make_map(x, colors=chroma::plasma, model="lab", interp="linear", ...)
}
