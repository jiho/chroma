% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hcl.R
\name{hcl}
\alias{hcl}
\alias{lch}
\title{HCL Color Specification}
\usage{
hcl(h = 0, c = 0.6, l = 0.6)

lch(l = 0.5, c = 0.5, h = 0)
}
\arguments{
\item{h}{hue, numeric vector with values in \code{[0, 360]}.}

\item{c}{chroma, numeric vector with values in \code{[0, 1]}; 0 is grey, 1 is full color.}

\item{l}{lightness, numeric vector with values in \code{[0, 1]}; 0 is black, 1 is white.}
}
\value{
A vector of colors specified as hex codes
}
\description{
Create a vector of colors from hue, chroma, and lightness.
}
\details{
The first argument can also be a data.frame or a matrix. In that case, its first three columns are considered as the three color components and the rest of the function arguments are ignored.

When the arguments are used separately and are vectors, values in shorter arguments are recycled to match the length of the longest argument. If the lengths are not compatible, an error is output.
}
\examples{
hcl()
hsl(330, 1, 0.6)
hsl(data.frame(c(330, 340), c(1,1), c(0.6, 0.4)))
hsl(matrix(c(330, 340, 1, 1, 0.6, 0.4), ncol=3))
hsl(c(330, 340), 1, 0.6)

# color ramps
ramp <- seq(0, 1, length.out=10)
show_col(hcl(h=ramp*360))
show_col(hcl(c=ramp))
show_col(hcl(l=ramp))

}
\seealso{
Other color.specifications: \code{\link{cmyk}},
  \code{\link{color_temperature}}, \code{\link{css_color}},
  \code{\link{hsl}}, \code{\link{hsv}}, \code{\link{lab}}
}

